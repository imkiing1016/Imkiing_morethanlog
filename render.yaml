# Render 블루프린트 — 실시간 WebSocket 서버.
# Render 대시보드에서 이 레포를 연결하면 아래 설정이 자동 적용된다.
services:
  - type: web
    name: bluffing-stock-game-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run server:start
    healthCheckPath: /
    autoDeploy: true
